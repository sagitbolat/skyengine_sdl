#include "SDL_sky.cpp"

/* Example: An implementation of Snake.
 * Design Goals: 
 * - A large grid of cells
 * - Movement with W,A,S,D keys
 * - A food randomly spawns in a cell
 * - When a player's head collides with a fruit, the fruit disappears and the player grows longer
 * - If a player
 */


void SkyInit() {
    return;
}
static void Start(GameState* game_state, KeyboardState* keyboard_state) {
    
    
    return;
}
static void Update(GameState* game_state, KeyboardState* keyboard_state) {
    return;
}